string = input("Enter a list of numbers separated by space: ")

nums = string.split()

nums = [int(num) for num in nums]

i = 0

while i < len(nums):

    if nums[i] % 3 == 0:

        nums.pop(i)

    else:

        i += 1

print(nums)

