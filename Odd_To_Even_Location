nums_string = input("Enter a list of numbers separated by space: ")

nums = [int(num) for num in nums_string.split()]

for i in range(0,len(nums)-1,2):

    nums[i], nums[i+1] = nums[i+1], nums[i]

print(nums)

